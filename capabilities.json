{
    "version": "1.0",
    "name": "BRINC",
    "description": "Pulls BRINC UAS drone telemetry into the TAK system and posts drone positions to the map.",
    "compute": {
        "memory": 512,
        "timeout": 60
    },
    "permissions": [
        {
            "resource": "feature:*",
            "required": true,
            "description": "BRINC posts drone positions to the map"
        }
    ],
    "invocations": {
        "incoming": {
            "schedule": {
                "description": "Polls BRINC for updated drone telemetry on a schedule.",
                "default": {
                    "enabled": true,
                    "schedule": "rate(1 minute)"
                }
            }
        }
    }
}
